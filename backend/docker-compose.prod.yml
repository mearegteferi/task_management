services:
  backend:
    volumes: [] # Disable code mounting in production
    environment:
      - NODE_ENV=production
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
    restart: always
